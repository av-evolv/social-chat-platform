name: Platform CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: platform-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      CI: '1'
      EXPO_NO_TELEMETRY: '1'
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
      - run: npx playwright install --with-deps chromium
      - name: Start platform
        run: |
          cp .env.example .env
          npm run oauth:setup
          docker compose up --build --detach --wait --wait-timeout 180
      - name: Verify OAuth protocol and PostgreSQL persistence
        env:
          OAUTH_TEST_DATABASE_URL: postgresql://larynx:larynx-local-only@127.0.0.1:5432/larynx
          OAUTH_TEST_BROWSER: '1'
        run: npm run test --workspace @larynx/api
      - run: npm run test:infra
      - run: npm run test:web
      - name: Verify database outage and recovery
        run: |
          docker compose stop postgres
          test "$(curl --silent --output /dev/null --write-out '%{http_code}' http://127.0.0.1:3000/health/ready)" = 503
          curl --fail http://127.0.0.1:3000/health/live
          docker compose start postgres
          docker compose up --detach --wait --wait-timeout 90 api
          curl --fail http://127.0.0.1:3000/health/ready
      - name: Logs on failure
        if: failure()
        run: docker compose logs --no-color --tail 150
      - name: Browser diagnostics
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: browser-diagnostics
          path: test-results/
          if-no-files-found: ignore
      - name: Stop CI infrastructure
        if: always()
        run: docker compose down --volumes
